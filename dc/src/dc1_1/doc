・Calenderクラス
日付の加減算を簡単に行うことができる。日付情報はDateクラスとして扱う。
Java7以下では、おすすめの方法だが、add()とset()で若干挙動が異なり、紛らわしい。

・LocalDateクラス
Java8以上で推奨の方法

・System.CurrentTimeMillis();
処理時間のような、極めて短い時間を正確に取得する際に役立つ。
日付計算などには向かない。